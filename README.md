# R&D / AI Assignment: Parametric Curve Fitting

This repository contains the solution for the Research and Development / AI assignment. The goal of this assignment is to find the unknown parameters of a given parametric equation to best fit a target dataset.

---

## 🚀 Quick Start & How to Run

There are two ways to run this project and verify the results.

### Option 1: Google Colab (Recommended)

You can view and run the complete, interactive solution directly in your browser with Google Colab. This requires no local setup.

**[Link to Your Google Colab Notebook]** <- *Paste your shared Colab link here*

### Option 2: Local Setup (e.g., VS Code)

If you have cloned this repository, you can run the solution locally.

**1. Prerequisites:**
* Python 3.7+
* The `xy_data.csv` file (included in this repo) in the same folder.

**2. Install Dependencies:**
The script relies on `pandas`, `numpy`, and `scipy`. You can install them using pip:
```bash
pip install pandas numpy scipy
